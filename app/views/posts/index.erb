<h1>Posts</h1>

<ol>
  <% @posts.each do |post| %>
    <li>
        <%= post.title %>
      </a>
      <p><%= post.content %></p>
      posted by <a ng-href="#/users/{{post.user.name}}">{{post.user.name}}</a>
    </li>
  <% end %>
</ol>
